'use client';

// Spline viewer removed — placeholder component kept to avoid import errors
import React from 'react';

interface SplineViewerProps {
  scene?: string;
  className?: string;
}

export function SplineViewer(_: SplineViewerProps) {
  // Component intentionally renders nothing now that the Spline viewer was removed.
  return null;
}
